{% extends 'base.html.twig' %}
{% set action = app.current_route == 'app_admin_produit_new' ? 'Ajout' : 'Édition' %}
{% block title %}Ajout d'un produit
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Ajout de produit</h1>

		{{ form_start(form) }}
		{{ form_widget(form) }}

		<button type="submit">Envoyer
			<button>
				{{ form_end(form) }}
			</div>
		{% endblock %}
